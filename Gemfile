# Gemfile (required for GitHub Pages builds)
source "https://rubygems.org"

# This pins Jekyll and all approved plugins for GitHub Pages
gem "github-pages", group: :jekyll_plugins

# You’re using this plugin in _config.yml
gem "jekyll-include-cache"
